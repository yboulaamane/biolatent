import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "BioLatent | Molecular & Protein Embedding Registry for Drug Discovery",
  description: "A standardized, searchable catalog and recommendation wizard for molecular representations and protein language model embeddings in ML drug discovery.",
  keywords: ["embeddings", "drug discovery", "protein language models", "molecular representations", "SMILES", "ESM-2", "ChemBERTa", "MolFormer", "bioinformatics", "chemoinformatics"],
  authors: [{ name: "BioLatent team" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <head>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
        <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&display=swap" rel="stylesheet" />
      </head>
      <body>
        {children}
      </body>
    </html>
  );
}
